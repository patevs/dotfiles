- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''

- link:
    ~/.dotfiles: ''
    ~/.profile:
        path: bash/.profile
        relink: true


- create:
    - ~/Desktop/git

- shell:
    - [git submodule update --init --recursive, Installing submodules]